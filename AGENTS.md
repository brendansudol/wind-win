# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Project notes

Read [docs/agent-notes.md](docs/agent-notes.md) before making changes — it covers the app's
core invariants (wind-direction convention, heading pipeline, Strava API budget) and the
environment gotchas (jest version pinning, SVG web compat, OAuth needing a dev build).
The product spec is [spec.md](spec.md).
